# 03. 給 QA / PG 的進階技術與除錯教學

本指南將指導開發人員 (PG) 與測試工程師 (QA) 如何進行進階設定、閱讀底層除錯資訊，以及如何微調 AI Agent 的行為。前提是您已完成 [01. 環境建置指南](01_Environment_Setup.md) 中關於 Python 環境 (`.venv`) 的設定。

---

## 🛠️ 第一部分：測試失敗如何 Debug？

當 PM / SA 回報「AI 測試結果判定為 Fail」或「AI 一直找不到按鈕」時，身為工程師的您可以透過以下方式排查。

### 1. 檢視 Walkthrough 紀錄 (`walkthrough.md`)
AI 的 Browser Subagent 在執行網頁操作時，會沿途紀錄每一步的足跡。
- **路徑**：打開 `.agent/test_artifacts/walkthrough_*.md`（例如 `walkthrough_test2.md`）。
- **重點看什麼**：搜尋 `Error:`、`Timeout:` 文字。常見問題是網路延遲導致元素還沒渲染，AI 就去點擊，造成 Timeout。

### 2. 檢視過程截圖與錄影
雖然最終的 HTML 報告很乾淨，但原始資源都保留在目錄下：
- **截圖**：`reports/screenshots/` (包含每一步的真實畫面)。
- **錄影紀錄**：`reports/videos/` (存放 `.webp` 格式的短影片)。若懷疑是動畫或轉場卡住，可直接播放短影片觀看。

### 3. 解讀 JSON 狀態檔
每次測試結束時，Agent 會產出暫存的 `reports/results.json` 或 `reports/test_cases.json` (若為 Word 測試)。裡面包含原始的布林值與字串狀態，可供寫自訂腳本分析時使用。

---

## 🎯 第二部分：輔助 AI 更精準地操作 (進階 Markdown)

如果網頁結構複雜 (例如好幾個名為「送出」的按鈕)，AI 可能會點錯。您可以在提供給 AI 的 Markdown 測試檔中，偷偷**加入 HTML 標籤或是 CSS Locator**，AI (Antigravity) 具備解讀工程提示的能力。

**較弱的指示 (PM 寫法)：**
> 點選畫面上的「登入」按鈕。

**強化的指示 (PG 寫法)：**
> 點選畫面右上角的「登入」按鈕。**[提示：CSS Selector 為 `button.login-header-btn`]**

加上括號提示後，底層的 Browser Agent 會優先掃描該 CSS 類別，大幅降低誤判率。這對於 Vue / React SPA 架構那些充滿動態 Class 的框架特別有用。

---

## ⚙️ 第三部分：如何自訂與擴充 AI Skill？

本專案的核心自動化測試邏輯定義在 `.agent/skills/` 中。

### 1. 修改 `autoTestReport` (負責處理 Markdown)
- 開啟 `.agent/skills/autoTestReport/SKILL.md`。
- 如果不想用預設的 HTML 佈景主題，去改寫它裡面呼叫的 `run_command` 指令，例如傳遞額外參數給 `generate_report.py`。

### 2. 修改 `browser-test-reporter` (負責處理 Word)
- 開啟 `.agent/skills/browser-test-reporter/SKILL.md`。
- 它包含了處理 `docx` 文件解析與寫回的邏輯，並且關聯到 `.agent/skills/browser-test-reporter/scripts/` 中的 Python 腳本。
- **提示**：如果您想更換生成的 Word 滿版底圖或表格樣式，請修改 `scripts/update_docx.py` 中的 `python-docx` 繪圖邏輯。

### 3. 測試報告產生器 (`generate_report.py`)
- Python 原始碼位於專案根目錄下的 `dev/generate_report.py`。
- 裡面定義了 HTML/CSS 的生成邏輯。若修改了這支程式碼，記得透過 `uv` 的 `pyinstaller` 重新編譯：
  ```bash
  # 重新打包成單檔 EXE
  pyinstaller --onefile dev/generate_report.py -n generate_report --distpath .agent/skills/autoTestReport/scripts/
  ```

---

## 💡 日常版控實用技 (收尾神技)

身為工程人員，當您完成除錯或修改了文件後：
1. 不必查 Git 狀態。
2. 直接對話框輸入：`「完成任務，幫我 wrap up」`。
3. AI 專屬技能 `wrap-up` 會自動讀取修改內容、生成繁體中文 Commits、Push 上 Git，並記錄此次您除錯的經驗至 `.gemini/tasks/lessons.md` 中。
