# 工程開發指南 (CONTRIBUTING)

本指南供工程人員參照，瞭解專案技術架構與開發 / 維護方式。

---

## 專案技術棧

| 元件 | 技術 | 說明 |
|---|---|---|
| 測試執行引擎 | AI Agent (Antigravity) | 透過 `browser_subagent` 執行瀏覽器自動化 |
| 報告產生器 | Python 3 + `markdown` 套件 | 將 walkthrough.md 轉為 HTML 報告 |
| 報告產生器（封裝） | PyInstaller → `generate_report.exe` | 提供給無 Python 環境的使用者使用 |
| 測試案例格式 | Markdown (`.md`) | 結構化三段式：階段一 / 執行步驟 / 預期結果 |

---

## 目錄結構

```
AutoTest/
├── .agent/                         # AI Agent 設定
│   ├── skills/
│   │   └── autoTestReport/         # SKILL 定義與執行檔
│   │       ├── SKILL.md            # 技能設定檔（觸發條件、步驟）
│   │       └── scripts/
│   │           └── generate_report.exe   # 報告產生器（編譯版）
│   └── test_artifacts/             # ⚠ 測試過程產物（.gitignore 排除）
│       └── walkthrough.md          # 每次測試的操作紀錄
├── .github/
│   └── workflows/                  # CI/CD 設定（預留）
├── Test/                           # 📝 測試案例存放區
│   ├── AutoTestReport_v1.md        # 早期版本（保留參考）
│   ├── AutoTestReport_v1_1.md      # 標準範例
│   └── AdvancedSearchTest.md       # 進階檢索測試案例
├── dev/                            # 🔧 開發用原始碼
│   └── generate_report.py          # 報告產生器 Python 原始碼
├── docs/                           # 📖 工程技術文件
│   └── architecture.md             # 系統架構設計
├── reports/                        # 📊 自動產生的報告（.gitignore 排除 HTML）
├── .gitignore
├── README.md                       # 非工程人員入口
├── SOP.md                          # 操作手冊
└── CONTRIBUTING.md                 # 本文件（工程開發指南）
```

---

## 開發環境設定

### 前置需求

- Python 3.10+
- [`uv`](https://docs.astral.sh/uv/)（Python 環境管理工具）

### 環境建置

```bash
# 使用 uv 建立虛擬環境
uv venv .venv

# 啟用虛擬環境 (Windows PowerShell)
.\.venv\Scripts\Activate.ps1

# 安裝依賴套件
uv pip install markdown
```

---

## 報告產生器

### 原始碼位置

`dev/generate_report.py`

### 運作邏輯

1. 讀取 `.agent/test_artifacts/walkthrough.md`
2. 將 Markdown 內容轉換為 HTML
3. 掃描 `.agent/test_artifacts/` 下的圖片，轉為 Base64 內嵌
4. 掃描影片檔（`.webm`、`.mp4`），轉為 Base64 嵌入
5. 套用 CSS 樣式，輸出至 `reports/Acceptance_Report_{時間戳記}.html`

### 手動執行（開發測試用）

```bash
# 使用 Python 原始碼（需在 .venv 環境下）
python dev/generate_report.py

# 使用封裝好的執行檔（不需 Python 環境）
.agent\skills\autoTestReport\scripts\generate_report.exe
```

### 編譯 EXE（更新執行檔）

```bash
# 安裝 PyInstaller
uv pip install pyinstaller

# 編譯為單一執行檔
pyinstaller --onefile dev/generate_report.py -n generate_report --distpath .agent/skills/autoTestReport/scripts/
```

---

## SKILL 開發

SKILL 定義檔位於 `.agent/skills/autoTestReport/SKILL.md`，採用 YAML Frontmatter + Markdown 格式。

### SKILL 架構

```yaml
---
name: autoTestReport
description: 自動化驗收測試 - 執行網站功能驗收並產生報告
---
```

### 觸發關鍵字

- `執行驗收`
- `開始測試`
- `/test`
- `產生測試報告`

### 修改 SKILL 行為

編輯 `SKILL.md` 中的 `[執行步驟]` 區塊即可調整 AI Agent 的自動化流程。

---

## 測試案例撰寫指引（工程觀點）

測試案例必須遵循三段式結構（詳見 `SOP.md`），此處補充工程注意事項：

1. **CSS Selector 提示**：若 UI 元素不易辨識，可在步驟中加入 CSS Selector 或 XPath 提示，協助 AI Agent 精準定位。
2. **等待策略**：對於 AJAX 載入頁面，明確註明等待條件（如「等待搜尋結果列表出現」）。
3. **異常路徑**：使用「處理異常路徑」標記，讓 AI Agent 知道遇到錯誤時的應對方式。
4. **紀錄點**：每個關鍵畫面變化處加入 `自動化紀錄點`，確保截圖完整覆蓋。

---

## Git 版控注意事項

- `reports/*.html` 與 `.agent/test_artifacts/` 下的產物已被 `.gitignore` 排除
- `generate_report.exe`（約 7MB）保留於版控中
- commit 訊息請使用繁體中文
